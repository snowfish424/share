# 包根 `scripts/`

`seed-data.sh` 會轉呼叫 `corrdiff/scripts/seed-data.sh`（方便在包根執行）。

其餘請進入對應路線目錄：

```bash
# Quickstart
cd quickstart && ./scripts/deploy-pipeline.sh

# CorrDiff
cd corrdiff
./scripts/deploy.sh
./scripts/seed-data.sh
```
